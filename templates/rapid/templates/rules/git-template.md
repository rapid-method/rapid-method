# Git Rules

---
project_name: '{project_name}'
last_updated: '{date}'
category: git
---

## Rule: Branch Naming
**Applies to**: All branches

| Type | Pattern | Example |
|------|---------|---------|
| Feature | `{feature_pattern}` | `{feature_example}` |
| Fix | `{fix_pattern}` | `{fix_example}` |
| Chore | `{chore_pattern}` | `{chore_example}` |
| Release | `{release_pattern}` | `{release_example}` |

## Rule: Commit Messages
**Applies to**: All commits

Format:
```
{commit_format}
```

Types: {commit_types}

Examples:
{commit_examples}

## Rule: Pull Requests
**Applies to**: All PRs

{pr_rules}

## Rule: Protected Branches
**Applies to**: Main branches

{protected_branch_rules}
